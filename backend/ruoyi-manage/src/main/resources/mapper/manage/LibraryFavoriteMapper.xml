<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.manage.mapper.LibraryFavoriteMapper">

    <select id="exists" resultType="int">
        select count(1) from tb_library_book_favorite where book_id = #{bookId} and user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.ruoyi.manage.domain.LibraryFavorite" useGeneratedKeys="true" keyProperty="id">
        insert into tb_library_book_favorite(book_id, user_id, create_by, create_time, del_flag)
        values(#{bookId}, #{userId}, #{createBy}, #{createTime}, '0')
    </insert>

    <delete id="delete">
        delete from tb_library_book_favorite where book_id = #{bookId} and user_id = #{userId}
    </delete>

    <!-- moved under mapper/manage for consistency -->
</mapper>
